import React, { ReactNode } from 'react';

interface LoginRegiserLayoutProps {
    children: ReactNode,
    title: string,
    width: number,
    height: number
}

export default LoginRegiserLayoutProps