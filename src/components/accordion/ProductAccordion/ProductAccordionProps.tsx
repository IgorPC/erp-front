interface ProductAccordionProps {
    id: number,
    name: string,
    code: string,
    createdBy: string,
    quantity: number,
    status: string,
    updatedAt: string,
    createdAt: string
    price: number
}

export default ProductAccordionProps